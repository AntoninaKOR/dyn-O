# ============================== SAM2 ==============================
numpy==1.26.3
tqdm
hydra-core
iopath==0.1.10
pillow==10.2.0
h5py

huggingface_hub
# only needed for video_track_all_obj.py --visualize; 5.x requires numpy>=2, which breaks procgen/gym3/numba
opencv-python-headless<5

-e segment-anything-2

# ============================= ProcGen =============================
procgen @ https://github.com/kaixin96/procgen-pyramid/releases/download/0.10.7/procgen-0.10.7+224x224-cp310-cp310-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
minari[create]==0.5.0
gym==0.23.0

# ============================== SOLV ==============================
timm==1.0.8
scipy==1.14.0
scikit-learn==1.5.1
torch --index-url https://download.pytorch.org/whl/cu121
torchvision --index-url https://download.pytorch.org/whl/cu121
tyro
mlflow
azureml-mlflow
comet_ml                # only needed for --logger comet
tensorboard
transformers
xformers --index-url https://download.pytorch.org/whl/cu121

# ============================== Cosmos ==============================
loguru>=0.7.0
mediapy==1.1.6
einops
einx

-e encoder/cosmos

# ============================== Mamba ==============================
# 2.3.2.post1 requires triton>=3.5.0, which torch 2.5.1 (pinned to triton 3.1.0) cannot
# satisfy: pip resolves the conflict by pulling a much newer torch and breaking the env.
# Build it separately, after torch, with --no-build-isolation.
mamba-ssm[causal-conv1d]<2.3.2

# ============================== Misc ==============================
numba==0.60.0